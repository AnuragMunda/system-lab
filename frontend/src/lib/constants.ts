export const EMAIL_PATTERN = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

// Decorative brand/accent colors used by the landing marketing sections
// (how-it-works step diagram and the architecture-preview mock screens).
export const BRAND_ACCENTS = {
  primary: "#3dd68c",
  cyan: "#22d3ee",
  warning: "#f59e0b",
  purple: "#a78bfa",
  danger: "#ef4444",
  muted: "#5b5b6d",
} as const;
